#include<stdio.h>
#include<string.h>

int find_comma_or_point(char * s, int offset);
void remove_blank(char * s, int position);

int main(){

char frase[1001];
char *p;
int cont;

printf("Informe a string:\n");
gets(frase);

cont=strlen(frase);

p=frase;

int find_comma_or_point(p,cont);

printf("%s",frase);



return 0;
}

int find_comma_or_point(char * s, int offset){
	
    for(int i=0;i<offset;i++){
      if(strlen(s)
	}

}
void remove_blank(char * s, int position){
	
	
     
}




